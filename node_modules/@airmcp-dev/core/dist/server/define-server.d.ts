import type { AirServerOptions, AirServer } from '../types/server.js';
/**
 * MCP 서버를 정의한다.
 *
 * 내장 기능 (자동 적용):
 * - 입력 검증 (zod)
 * - 에러 → MCP 프로토콜 변환
 * - 로깅 (builtin logger plugin)
 * - 보안 (shield 설정 시: 정책, 위협탐지, 레이트리밋, 감사)
 * - 측정 (meter 설정 시: 7-Layer 분류, 호출 추적, 지연시간)
 * - Graceful shutdown (SIGTERM/SIGINT)
 */
export declare function defineServer(options: AirServerOptions): AirServer;
//# sourceMappingURL=define-server.d.ts.map