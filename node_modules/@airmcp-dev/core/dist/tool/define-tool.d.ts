import type { AirToolDef, AirToolParams, AirToolHandler } from '../types/tool.js';
/**
 * MCP 도구를 정의한다.
 *
 * @example
 * ```ts
 * const search = defineTool('search', {
 *   description: '고객 검색',
 *   params: { query: 'string', limit: 'number?' },
 *   handler: async ({ query, limit }) => {
 *     return db.find({ query }).limit(limit ?? 10);
 *   },
 * });
 * ```
 */
export declare function defineTool(name: string, options: {
    description?: string;
    params?: AirToolParams;
    handler: AirToolHandler;
    layer?: number;
    tags?: string[];
}): AirToolDef;
//# sourceMappingURL=define-tool.d.ts.map