import type { AirMiddleware } from '../types/middleware.js';
/** air 전용 에러 클래스 — 에러 코드와 메타데이터를 포함 */
export declare class AirError extends Error {
    code: number;
    details?: Record<string, any>;
    constructor(message: string, code?: number, details?: Record<string, any>);
}
/** MCP 표준 에러 팩토리 */
export declare const McpErrors: {
    /** 도구를 찾을 수 없음 */
    toolNotFound: (name: string) => AirError;
    /** 파라미터 검증 실패 */
    invalidParams: (message: string, params?: Record<string, any>) => AirError;
    /** 내부 서버 에러 */
    internal: (message: string, cause?: Error) => AirError;
    /** 접근 거부 */
    forbidden: (message: string) => AirError;
    /** 레이트 리밋 초과 */
    rateLimited: (tool: string, retryAfterMs?: number) => AirError;
    /** 위협 감지 */
    threatDetected: (type: string, severity: string) => AirError;
    /** 타임아웃 */
    timeout: (tool: string, timeoutMs: number) => AirError;
};
/** 에러 → MCP 응답 변환 미들웨어 */
export declare function errorBoundaryMiddleware(): AirMiddleware;
//# sourceMappingURL=error-handler.d.ts.map