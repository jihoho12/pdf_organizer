import type { AirPlugin } from '../../types/plugin.js';
interface RetryOptions {
    /** 최대 재시도 횟수 (기본: 3) */
    maxRetries?: number;
    /** 초기 대기 시간 ms (기본: 200) */
    delayMs?: number;
    /** 재시도할 에러 패턴 (기본: 전부) */
    retryOn?: (error: Error) => boolean;
}
export declare function retryPlugin(options?: RetryOptions): AirPlugin;
export {};
//# sourceMappingURL=retry.d.ts.map