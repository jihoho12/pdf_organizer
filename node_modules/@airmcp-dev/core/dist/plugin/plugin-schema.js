// @airmcp-dev/core — plugin/plugin-schema.ts
// AirPlugin 유효성 검증
/** 플러그인 또는 팩토리를 AirPlugin 인스턴스로 정규화 */
export function resolvePlugin(input) {
    if (typeof input === 'function') {
        return input();
    }
    return input;
}
/** 플러그인 기본 검증 */
export function validatePlugin(plugin) {
    const errors = [];
    if (!plugin.meta?.name)
        errors.push('plugin.meta.name is required');
    return errors;
}
//# sourceMappingURL=plugin-schema.js.map